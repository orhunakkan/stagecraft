services:
  app:
    build: .
    ports:
      - '3001:3001'
    environment:
      NODE_ENV: production
      PORT: 3001
      # Set SESSION_SECRET in a .env file or via the host environment.
      # Never commit a real secret here.
      SESSION_SECRET: ${SESSION_SECRET:?SESSION_SECRET is required}
      # Optional — only used by the audit-log-search lab. Unset by default so
      # docker compose up keeps working on the lab's in-memory fallback store.
      AZURE_SQL_CONNECTION_STRING: ${AZURE_SQL_CONNECTION_STRING:-}
      # This container is the practice site's automation target, not a real production
      # deployment, so the audit-log-search lab's reseed action stays enabled here.
      AUDIT_LOG_ALLOW_RESEED: 'true'
    restart: unless-stopped
    healthcheck:
      test:
        [
          'CMD',
          'node',
          '-e',
          "fetch('http://localhost:3001/ready').then(r=>r.ok||process.exit(1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      start_period: 10s
      retries: 3
