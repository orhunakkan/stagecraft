PORT=3001
SESSION_SECRET=change-me-in-production
CLIENT_ORIGIN=http://localhost:5173
NODE_ENV=development
# Optional for local dev/CI — only needed for the book-catalog lab's real persistence, see docs/azure-sql.md.
# Leave unset to use the lab's in-memory fallback store. The deployed practice site always has this configured.
AZURE_SQL_CONNECTION_STRING=
