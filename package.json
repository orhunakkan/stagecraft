{
  "name": "stagecraft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "lint": "node -e \"throw new Error('lint is not configured yet')\"",
    "format:check": "node -e \"throw new Error('format check is not configured yet')\"",
    "test": "node -e \"throw new Error('tests are not configured yet')\"",
    "test:e2e": "node -e \"throw new Error('e2e tests are not configured yet')\"",
    "test:e2e:ui": "node -e \"throw new Error('e2e UI mode is not configured yet')\"",
    "verify": "npm run lint && npm run typecheck && npm test -- --coverage && npm run test:e2e && npm run build"
  },
  "dependencies": {
    "next": "^16.2.4",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "@types/react": "^19.2.7",
    "@types/react-dom": "^19.2.3",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "postcss": "^8.5.13"
  }
}

