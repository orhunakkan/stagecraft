{
  "name": "stagecraft",
  "version": "1.0.0",
  "description": "Advanced playground for Playwright testing framework",
  "license": "ISC",
  "author": "Orhun Akkan",
  "private": true,
  "engines": {
    "node": "24.x"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev --workspace=client\" \"npm run dev --workspace=server\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "build": "npm run build --workspace=client && npm run build --workspace=server",
    "test": "npm run test --workspace=client && npm run test --workspace=server",
    "test:run": "npm run test:run --workspace=client && npm run test:run --workspace=server",
    "test:coverage": "npm run test:coverage --workspace=client && npm run test:coverage --workspace=server",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "lint": "eslint client/src server/src e2e",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "package:azure": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package-azure-app-service.ps1",
    "start": "node server/dist/index.js",
    "typecheck": "tsc --noEmit && npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "prepare": "husky || true"
  },
  "overrides": {
    "shell-quote": "^1.10.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "@types/node": "^25.9.1",
    "@vitest/coverage-v8": "^4.1.11",
    "concurrently": "^10.0.5",
    "eslint": "^10.9.0",
    "eslint-plugin-import-x": "^4.16.2",
    "eslint-plugin-jsx-a11y": "6.10.2",
    "eslint-plugin-react-hooks": "^7.1.1",
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "3.9.6",
    "typescript-eslint": "^8.67.0"
  }
}
